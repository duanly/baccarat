# 复制为 deploy/.env 并填好
JWT_SECRET=换成一段随机长字符串
DEALER_API_KEY=换成荷官设备用的密钥
ADMIN_USER=admin
ADMIN_PASS=换成强密码
SIGNUP_BONUS=0
PUBLIC_IP=服务器公网IP
# 前置代理：caddy（本机 80/443 空闲）或 external（80/443 已被别的项目占用，用已有 Nginx/Caddy 转发）
FRONT=external
